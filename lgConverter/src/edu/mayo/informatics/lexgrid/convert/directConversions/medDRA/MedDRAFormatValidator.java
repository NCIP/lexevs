package edu.mayo.informatics.lexgrid.convert.directConversions.medDRA;import java.io.File;import java.net.URI;public class MedDRAFormatValidator {    public static boolean isValidDirecotry(URI sourceDir) {        File dir = new File(sourceDir);        return dir.exists();    }    public static boolean allMedDRAfilesExist(URI sourceDir) {        boolean allExist = true;        String input;                for(int i=0; i < MedDRA2LGConstants.medDRADataFiles.length; i++){            input = sourceDir + MedDRA2LGConstants.medDRADataFiles[i];            File file = new File(input);            if(!file.exists()){                allExist = false;            }        }        return allExist;    }    public static boolean allMedDRAfilesValid(URI medDRADirectory) {        // TODO Auto-generated method stub        // TODO: check each file for valid format....        return true;    }}