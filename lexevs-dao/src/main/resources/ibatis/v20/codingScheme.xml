<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL MAP 2.0//EN" 
	"http://www.ibatis.com/dtd/sql-map-2.dtd">
<sqlMap namespace="CodingScheme">

	<typeAlias alias="codingScheme" type="org.LexGrid.codingSchemes.CodingScheme" />
	<typeAlias alias="codingSchemeSummary" type="org.LexGrid.LexBIG.DataModel.Core.CodingSchemeSummary" />

	<resultMap id="codingSchemeResult" class="codingScheme" groupBy="codingSchemeName,representsVersion">
		<result property="codingSchemeName" column="codingSchemeName" />
		<result property="codingSchemeURI" column="codingSchemeUri" />
		<result property="representsVersion" column="representsVersion" />
		<result property="formalName" column="formalName" />
		<result property="defaultLanguage" column="defaultLanguage" />
		<result property="approxNumConcepts" column="approxNumConcepts" />
		<result property="entityDescription.content" column="description" />
		<result property="copyright.content" column="copyright" />
		<result property="isActive" column="isActive" />
		<result property="entryState" resultMap="entryStateResult" />
		<result property="_sourceList" resultMap="CodingScheme.sourceResult"/>
		<result property="_localNameList" resultMap="CodingScheme.localNameResult"/>
	</resultMap>

	<resultMap id="codingSchemeSummaryResult" class="codingSchemeSummary">
		<result property="localName" column="codingSchemeName" />
		<result property="codingSchemeURI" column="codingSchemeUri" />
		<result property="representsVersion" column="representsVersion" />
		<result property="formalName" column="formalName" />
		<result property="codingSchemeDescription.content" column="description" />
	</resultMap>
	
	<delete id="deleteCodingSchemeById" parameterClass="org.lexevs.dao.database.ibatis.parameter.PrefixedParameter">
		DELETE FROM
			$prefix$codingScheme
		WHERE
			codingSchemeGuid = #param1#
	</delete>
	
	<select id="getCodingSchemeById" parameterClass="org.lexevs.dao.database.ibatis.parameter.PrefixedParameter" resultMap="CodingScheme.codingSchemeResult">
		SELECT
			cs.codingSchemeGuid,
			cs.codingSchemeName,
			cs.codingSchemeUri,
			cs.representsVersion,
			cs.formalName,
			cs.defaultLanguage,
			cs.approxNumConcepts,
			cs.description,
			cs.copyright,
			cs.isActive,
			cs.owner,
			cs.status,
			cs.effectiveDate,
			cs.expirationDate,
			es.changeType,
			es.relativeOrder,
			es.revisionGuid,
			es.prevRevisionGuid,
			rev.revisionGuid,
			csma.*
		FROM
			$prefix$codingScheme cs
			
		LEFT JOIN
			$prefix$entryState es
		ON
			cs.entryStateGuid = es.entryStateGuid
			
		LEFT JOIN
			$prefix$revision as rev
		ON
			es.revisionGuid = rev.revisionGuid
			
		LEFT OUTER JOIN
			$prefix$csMultiAttrib as csma
		ON
			csma.codingSchemeGuid = cs.codingSchemeGuid

		WHERE
			cs.codingSchemeGuid = #param1#

     </select>
     
    <select id="getCodingSchemeSummaryByUriAndVersion" parameterClass="org.lexevs.dao.database.ibatis.parameter.PrefixedParameterTuple" resultMap="codingSchemeSummaryResult">
		SELECT
			codingSchemeName,
			codingSchemeURI,
			representsVersion,
			formalName,
			description
		FROM 
			$prefix$codingScheme
		WHERE
			codingSchemeURI =
			#param1#
		AND
			representsVersion =
			#param2#
	</select>
	
	<select id="getCodingSchemeByNameAndVersion" parameterClass="org.lexevs.dao.database.ibatis.parameter.PrefixedParameterTuple" resultMap="codingSchemeResult">
		SELECT
			codingSchemeGuid,
			codingSchemeName,
			codingSchemeUri,
			representsVersion,
			formalName,
			defaultLanguage,
			approxNumConcepts,
			description,
			copyright,
			isActive,
			owner,
			status,
			effectiveDate,
			expirationDate,
			es.changeType,
			es.relativeOrder,
			es.revisionGuid,
			es.prevRevisionGuid,
			rev.revisionGuid
		FROM
			$prefix$codingScheme cs
			
		LEFT JOIN
			$prefix$entryState es
		ON
			cs.entryStateGuid = es.entryStateGuid
			
		LEFT JOIN
			$prefix$revision as rev
		ON
			es.revisionGuid = rev.revisionGuid

		WHERE
			cs.codingSchemeName = #param1#
		AND
			cs.representsVersion = #param2#
     </select>
     
     <select id="getCodingSchemeIdByNameAndVersion" parameterClass="org.lexevs.dao.database.ibatis.parameter.PrefixedParameterTuple" resultClass="string">
		SELECT
			codingSchemeGuid
		FROM
			$prefix$codingScheme
		WHERE
			codingSchemeName = #param1#
		AND
			representsVersion = #param2#
     </select>
     
     <select id="getCodingSchemeIdByUriAndVersion" parameterClass="org.lexevs.dao.database.ibatis.parameter.PrefixedParameterTuple" resultClass="string">
		SELECT
			codingSchemeGuid
		FROM
			$prefix$codingScheme
		WHERE
			codingSchemeUri = #param1#
		AND
			representsVersion = #param2#
     </select>
     
     <select id="getEntryStateId" parameterClass="map" resultClass="string">
		SELECT
			entryStateId
		FROM
			codingScheme
		WHERE
			codingSchemeName = #codingSchemeName#
		AND
			representsVersion = #representsVersion#
		AND
			isCurrentRelease = #isCurrentRelease#
     </select>
   
	<update id="updateCodingScheme" parameterClass="map">
		UPDATE codingScheme

		SET 
			<isNotNull property="codingScheme.codingSchemeName">
				codingSchemeName = #codingScheme.codingSchemeName#
			</isNotNull>
			<isNotNull prepend="," property="codingScheme.representsVersion">
				representsVersion = #codingScheme.representsVersion#
			</isNotNull>
			<isNotNull prepend="," property="codingScheme.approxNumConcepts">
				approxNumConcepts = #codingScheme.approxNumConcepts#
			</isNotNull>
		WHERE 
			codingSchemeName = #codingSchemeName#
		AND
			representsVersion = #representsVersion#
		AND
			isCurrentRelease = #isCurrentRelease#
 	</update>

	<insert id="insertCodingScheme" parameterClass="org.lexevs.dao.database.ibatis.codingscheme.parameter.InsertCodingSchemeBean">
		
		INSERT INTO $prefix$codingScheme (
			codingSchemeGuid,
			codingSchemeName,
			codingSchemeUri,
			representsVersion,
			formalName,
			defaultLanguage,
			approxNumConcepts,
			description,
			copyright,
			isActive,
			owner,
			status,
			effectiveDate,
			expirationDate,
			entryStateGuid
		) VALUES (
			#id#, 
			#codingScheme.codingSchemeName#, 
			#codingScheme.codingSchemeURI#, 
			#codingScheme.representsVersion#,
			#codingScheme.formalName#,
			#codingScheme.defaultLanguage#,
			#codingScheme.approxNumConcepts#,
			#codingScheme.entityDescription.content#,
			#codingScheme.copyright.content#,
			#codingScheme.isActive#,
			#codingScheme.owner#,
			#codingScheme.status#,
			#codingScheme.effectiveDate#,
			#codingScheme.expirationDate#,
			#entryStateId#
		)
  </insert>
     
    
</sqlMap>