Connection Helper contains a single class which does basically nothing - 
but it needs to be here as a jar file so my custom classloader can locate it
when it looks in the "JAR_FILE_LOCATION" variable locations.